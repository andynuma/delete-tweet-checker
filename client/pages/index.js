import {eth,getInstance} from "../web3/provider"
import {Checker} from "../web3/artifacts/Checker.json"

export default class IndexPage extends React.Component{
    render() {
        return (
            <h1>a</h1>
        )
    }
}
